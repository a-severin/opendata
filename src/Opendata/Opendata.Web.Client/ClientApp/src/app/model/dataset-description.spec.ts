import { DatasetDescription } from './dataset-description';

describe('DatasetDescription', () => {
  it('should create an instance', () => {
    expect(new DatasetDescription()).toBeTruthy();
  });
});
